list = [1,2,3,4,5]

for i in range(len(list)):
    print

def reversingList(array):

    result=[]*len(array)
    print(result)
    for i in range(1,len(array)):
        result[i]=array[len(array)-i]

    return result

print(reversingList(list))